<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TinyPlanet.fm</title>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <link href='http://fonts.googleapis.com/css?family=Syncopate' rel='stylesheet' type='text/css'>

  </head>

  <body>   <div id="signin-div" class="row" >
      <ul id="sign-in-btn">
        <% if user_signed_in? %>
          <li><%= link_to current_user.username, "/users/edit" %></li>
          <li><%= link_to "sign out", destroy_user_session_path, :method => :delete %></li>
        <% else %>
        <%= link_to "Login", "#login", "data-toggle" => "modal", :class => 'btn btn-small btn-success' %>

        <%= link_to "Sign up", "#sign_up", "data-toggle" => "modal", :class => 'btn btn-small btn-success' %>
          <%= render "users/login_modal" %>

          <%= render "users/sign_up_modal" %>
        <% end %>
      </ul>
    </div>




    <div class="container">
        <%= yield %> 
    </div>

    <div id="stripes-div">
      <div id="stripe3"></div>
      <div id="stripe2"></div>
      <div id="stripe1"></div> 
    </div>

  </body>
  
</html>